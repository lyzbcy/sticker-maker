# 平台批量同步与审核历史实施清单

依据：用户已批准 `doc/platform-sync-investigation-2026-09-07.md` 的优化方向，并要求修改当前版本。直接修改当前工作目录，保留已有未提交修改。

目标：用现有登录会话读取全量列表 JSON，按作品 ID 定位；发布不查驳回；同轮去重、新轮留历史。

- [ ] 添加列表协议、303 单同步、理由缓存、同理由新轮次、失败恢复及发布入口测试，先验证失败。
- [ ] 新建 `publish/platform_data.py` 负责只读数据校验、身份匹配、增量同步和审核历史；元数据添加兼容字段。
- [ ] `status.py` 接入快速读取，接口不可用时保留页面降级；状态只读模式禁止抓理由。
- [ ] 发布入口只使用本次同步确认的通过名单，按 ID 直达详情并核验状态；提交成功统一记录轮次。
- [ ] 前端显示补录/跳过/失败摘要、历史轮次，增加强制补录入口，平台任务互斥。
- [ ] 运行后端与前端测试、构建；用登录态只读样本验证解析及缓存，不实际发布作品。
- [ ] 完成代码复查、更新文档与验证记录。

平台外同日重提且未观测中间状态不能自动确认轮次，界面保留说明与强制补录。未知旧轮次使用“历史记录”，不推断实际第一次审核。
