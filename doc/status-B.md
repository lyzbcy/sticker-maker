# B Mac 桌面应用 · 状态

> **何时读**：修改 Electron/Vue、首次向导、安装、打包或自动更新时。

## 已完成

- 首次向导：Codex 检测、base 与概率、模式概率、角色概率、生图偏好。
- 保存后默认进入大本营；设置页可重新调整。
- 生图进度/ETA、取消、结果预览、打开目录与微信提交。
- 设置、Agent 工具、近 50 条内存日志、介绍/推广页面。
- 启动自动检查更新与手动检查；语义版本、HTTPS、SHA-256、下载、解压、安装替换。
- ZIP 内含 `.app` 和 `install.command`；安装脚本处理隔离、替换、权限与启动。

## 本轮发现并修复的打包边界缺陷

普通组件测试使用了浏览器 stub，因此没有暴露 Electron sandbox 中 `pathToFileURL` 不可用的问题。真实打包走查发现首次向导 base 页空白后，改为预加载脚本内置安全路径编码，并新增中文、空格、`#`、`?` 回归测试。

## 验证

- 14 个桌面测试通过。
- Vite production build 55 modules。
- electron-builder arm64 打包成功。
- 真实 `.app` 启动并走到 base 页；12 张本地图全部加载，控制台零异常。
- 整个应用 ad-hoc 深度签名并通过严格校验。
- 最终安装器在隔离临时目录复制安装并通过签名复验。

## 分发

- `desktop/表情包一键制作-mac-v0.2.0.zip`
- 208 MB
- SHA-256：`5edf4a1a4fd9e2d8378304f2581ba9e97c06ed1c864a56a6868e55205686737a`

## 已知边界

- 当前仅 Apple Silicon。
- 没有 Apple Developer ID 公证；通过 ad-hoc 签名与安装脚本清除下载隔离。
- 正式发布前应在另一台非开发机从 ZIP 下载开始复验。
