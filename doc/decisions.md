# 关键设计决策记录

> **何时读**：想知道某个设计为什么这么定。

完整 spec 第 1 节有 13 个决策的详细理由，这里是要点速查。

| # | 决策点 | 选择 | 一句话理由 |
|---|---|---|---|
| A1 | codex 依赖 | **用户自备 + 安装引导** | 不担 token 费用/风控 |
| B1 | 角色数据模型 | **内置 4 角色 + base 图 + 默认概率** | 开箱即用 |
| C | 参考图库 | **纯空，用户自建，不分发** | 参考图是个人素材 |
| D1 | 剧本/关键词库 | **内置 174 剧本 + 关键词库** | 方法论资产，分发增值 |
| E1 | 抠图策略 | **全自动（洋红优先 + hue-guard + 绿色回退）** | 与现有 skill 一致 |
| F1 | A 交付形态 | **纯 Python 核心库** | 职责清晰，上层解耦 |
| G1 | 现有脚本处置 | **重构重写，保留 know-how** | 质量 + 跨平台 |
| H1 | 抠图实现 | **核心库内置（Pillow+numpy）** | 解耦 codex |
| J1 | 生成 base 图 | **纳入 A（codex 复用）** | 不重复造轮 |
| K | AI 能力来源 | **全部走 codex**（含义预检 + 介绍文案） | 复用 A1 依赖 |
| L | 剧本池耗尽 | **降级排列组合 + WARN** | 简单可控 |
| M | 动态表情 | **A 不做，架构预留** | 主线是静态 |
| N | 软件名字 | **对外"表情包一键制作"，代码 sticker_engine** | 中文对外，ASCII 代号避坑 |

## 资源归属（什么随软件分发）

| 资源 | 分发 |
|---|---|
| 4 角色 + base 图 + 默认概率 | ✅ 内置 |
| 联动剧本库（174 组） | ✅ 内置 |
| 关键词库 | ✅ 内置 |
| 参考图库 | ❌ 纯空，用户自建 |
| codex CLI | ❌ 用户自备 |

## 架构选择：管线编排（方案1）

3 选 1 选了管线编排，理由：**关卡门禁做成架构一等公民**——这正是现有 skill 用 7 必经步骤 + 生产日志死守的东西。
